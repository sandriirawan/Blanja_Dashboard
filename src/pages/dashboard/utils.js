// utils.js (Buat berkas utilitas Anda sendiri)
const Utils = {
    months: (options) => {
      // Logika untuk menghasilkan data bulan
    },
    numbers: (options) => {
      // Logika untuk menghasilkan data angka
    },
    transparentize: (color, alpha) => {
      // Logika untuk membuat warna transparan
    },
    CHART_COLORS: {
      red: 'rgb(255, 0, 0)',
      orange: 'rgb(255, 165, 0)',
      // Definisikan warna lain yang Anda butuhkan
    }
  };
  
  export default Utils;
  